class Post {
  String name, location, number, description, date, time, logo, address;

  Post({this.name, this.location, this.number, this.description, this.date, this.time, this.logo, this.address});  // constructor
}
